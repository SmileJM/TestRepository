<?xml version="1.0" encoding="UTF-8"?>

<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="boardcomment">

	<insert id="insert" parameterType="com.mycompany.myapp.dto.BoardComment">
		<selectKey keyProperty="bcno" resultType="int" order="AFTER">
		select boardcomment_bcno_seq.currval from dual
		</selectKey>
		insert into boardcomment (bno, bcno,  bcwriter, bcdate, bccomment, bcpassword )
		values (#{bno}, boardcomment_bcno_seq.nextval, #{bcwriter}, sysdate, #{bccomment}, #{bcpassword}	)
	</insert>
	
	<select id="selectcommentlist" parameterType="com.mycompany.myapp.dto.BoardComment"
		resultType="com.mycompany.myapp.dto.BoardComment">
		select 
			bccomment, 
			<!-- (select distinct mname from member a, boardcomment b where a.MEMAIL = b.BCWRITER and bno=b.bno) as bcwriter, -->
			bcwriter,
			bcdate 
		from boardcomment 
		where bno = #{bno} 
		order by bcno asc	
	</select>
</mapper>